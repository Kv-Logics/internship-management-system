events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name cdi.nitt.edu;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            # Redirect to HTTPS once SSL is set up.
            # For now, it will serve HTTP traffic.
            # return 301 https://$host$request_uri;
            
            proxy_pass http://frontend:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location /api/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
        
        location /docs {
            proxy_pass http://backend:8000;
        }
        
        location /openapi.json {
            proxy_pass http://backend:8000;
        }
    }
    
    # Uncomment the following block AFTER running certbot to enable HTTPS
    # server {
    #     listen 443 ssl;
    #     server_name cdi.nitt.edu;
    #
    #     ssl_certificate /etc/letsencrypt/live/cdi.nitt.edu/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/cdi.nitt.edu/privkey.pem;
    #
    #     location / {
    #         proxy_pass http://frontend:3000;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #     }
    #
    #     location /api/ {
    #         proxy_pass http://backend:8000;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #     }
    # }
}
